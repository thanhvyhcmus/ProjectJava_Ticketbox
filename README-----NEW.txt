ALTER TABLE `ticketbox`.`user` 
CHANGE COLUMN `favouriteGenre` `favouriteGenre` VARCHAR(30) NULL DEFAULT NULL ;

bởi vì kích thước của genrnre nhỏ nên mn chạy dòng này để chiều chỉnh lại kích thước dùm t nha
